import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
  },
  input: {
    flex: 1,
    margin: 6,
  },
  inputContent: { paddingHorizontal: 6, textAlign: 'center' },
});

export default styles;
