import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: { flexDirection: 'row' },
  icon: { alignSelf: 'flex-end', marginHorizontal: 8 },
});

export default styles;
